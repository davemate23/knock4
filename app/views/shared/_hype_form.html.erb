<%= bootstrap_form_for [@hypeable, Hype.new] do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
    <%= f.text_area :content, placeholder: "Create some Hype..." %>
  <%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>