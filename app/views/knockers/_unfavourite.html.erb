<%= form_for(current_knocker.favouriteknockers.find_by(favourite_id: @knocker.id),
             html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Remove from Favourites", class: "btn btn-large" %>
<% end %>
